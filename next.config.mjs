/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/ecommerce-website-v1",
  output: "export", 
    images: {
        domains: ['picsum.photos','placehold.jp','www.aristobrat.in']
      }

};

export default nextConfig;
